<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:LolClientHelper.ViewModels"
             x:Class="LolClientHelper.Views.EndOfGameView"
             x:DataType="viewModels:EndOfGameViewModel">
    <Border Style="{DynamicResource GlassCard}" Padding="16">
        <VerticalStackLayout Spacing="16">
            <Label Text="{Binding EndOfGameTabText}" Style="{DynamicResource GlassTitle}" />
            
            <VerticalStackLayout Spacing="12">
                <Border Style="{DynamicResource GlassItem}">
                    <HorizontalStackLayout Spacing="12">
                        <CheckBox IsChecked="{Binding AutoSendEndOfGameMessage}" />
                        <Label VerticalOptions="Center" Text="{Binding AutoSendLabel}" Style="{DynamicResource GlassLabel}" />
                    </HorizontalStackLayout>
                </Border>

                <Border Style="{DynamicResource GlassItem}" Padding="12">
                    <Editor 
                        Text="{Binding EndOfGameMessage, Mode=TwoWay}"
                        Placeholder="{Binding MessagePlaceholder}"
                        AutoSize="TextChanges"
                        HeightRequest="100" />
                </Border>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </Border>
</ContentView>
